import React from 'react';
import ReactDOM from 'react-dom/client';
import Start from './intro/intro';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <Start />
  </React.StrictMode>
);


